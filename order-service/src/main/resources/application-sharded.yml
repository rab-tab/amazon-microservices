spring:
  config:
    activate:
      on-profile: sharded
  datasource:
    driver-class-name: org.apache.shardingsphere.driver.ShardingSphereDriver
    url: jdbc:shardingsphere:classpath:sharding-config.yaml
  flyway:
    enabled: false   # ShardMigrationConfig handles per-shard migration instead