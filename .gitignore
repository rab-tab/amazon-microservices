# Maven build output
target/
*.class
*.jar
*.war

# IDE files
.idea/
*.iml
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS files
.DS_Store
Thumbs.db

# Environment / secrets — NEVER commit these
.env
*.pem
*.key
secrets/

# Docker volumes (if accidentally created locally)
docker-volumes/

# Logs
*.log
logs/

# Exception: k8s test manifest contains only placeholder credentials
# (amazon/amazon123/redis123 — confirmed via base64 decode, not real secrets)
\!k8s/secrets/secrets.yaml
